import { FC } from 'react'
import { Element } from './style';
// TODO: LAZY LOAD
const LazyBackgroundImg: FC = () => {
    return (
        <Element></Element>
    );
}

export default LazyBackgroundImg