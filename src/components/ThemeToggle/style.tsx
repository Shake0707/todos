import styled from "styled-components";

export const Btn = styled.label`
    width: 25.113px;
    height: 26px;
    background-image: var(--themeToggle-img);
    cursor: pointer;

    &>input {
        display: none;
    }
`